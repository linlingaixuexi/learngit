class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        k %= len(nums)
        l = len(nums)
        temp = []
        temp = nums[l-k:] + nums[:l-k]
        for i in range(len(temp)):
            nums[i] = temp[i]
